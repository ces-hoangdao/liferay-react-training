import { createTheme } from "@mui/material"

const theme = createTheme({
  palette: {
    button: {
      light: "#ECB390",
      dark: "#e59766",
    },
    table: {
      main: "94B49F",
    },
    input: {
      main: "#FCF8E8",
      secondary: "#94B49F",
    },
  },
})

export default theme
